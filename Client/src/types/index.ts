export enum Status {
    review = "review",
    active = "active",
    in_progress = "in_progress",
    resolved = "resolved",
    closed = "closed"
}

export enum Priority {
    low = "low",
    medium = "medium",
    high = "high",
    critical = "critical"
}

export enum Type {
    bug = "bug",
    feature = "feature"
}

