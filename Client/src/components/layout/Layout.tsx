import Provider from "../../utils/context";
import Wrapper from "./Wrapper";

const Layout = () => {
    return (
        <Provider>
            <Wrapper />
        </Provider>
    )
}

export default Layout;
